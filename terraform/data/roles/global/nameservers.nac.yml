iosxr:
  global:
    variables:
    # Infrastructure parameters
      domain_name: "cml.transit32.local"
      dns_server_primary: "100.71.72.254"
    configuration:
    # Infrastructure configuration
      domain:
        name: "${domain_name}"
        lookup_disable: true
        name_servers:
          - address: "${dns_server_primary}"