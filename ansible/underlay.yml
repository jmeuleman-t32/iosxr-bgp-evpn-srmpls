---
# main playbook
- hosts: ic-core, ic-edge
  gather_facts: false
  roles:
    - role: underlay
- hosts: all
  gather_facts: false
  tasks:
    - name: Save Running-Config to Startup-Config
      cisco.nxos.nxos_config:
        save_when: always